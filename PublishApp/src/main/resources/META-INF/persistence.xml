<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0" xmlns="https://jakarta.ee/xml/ns/persistence" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">
    <!-- Define Persistence Unit -->
    <persistence-unit name="myPublish" transaction-type="JTA">
        <!-- EclipseLink provider -->
        <provider>org.eclipse.persistence.jpa.PersistenceProvider</provider> 
        
        <!-- Use your configured JDBC DataSource -->
        <jta-data-source>jdbc/ictPublishApp</jta-data-source>

        <properties>
            <!-- Schema generation -->
            <property name="jakarta.persistence.schema-generation.database.action" value="None"/>
            
            <!-- Validation -->
            <property name="jakarta.persistence.validation.mode" value="AUTO"/>
        </properties>
    </persistence-unit>
</persistence>
